import { anchor, wrapper } from "design-system/styles/BackToTop.css";

export const BackToTop = () => (
  <div className={wrapper}>
    <a href="#" className={anchor}>
      Back to top
    </a>
  </div>
);
